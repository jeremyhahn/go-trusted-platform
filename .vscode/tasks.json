{
    "version": "2.0.0",
    "tasks": [
    {
        "type": "shell",
        "label": "debug_client_config_clean",
        "command": "rm -rf pkg/trusted-data && mkdir -p pkg/trusted-data/etc && cp configs/platform/config.dev.client.yaml pkg/trusted-data/etc/config.yaml",
    },
    {
        "type": "shell",
        "label": "debug_server_config_clean",
        "command": "rm -rf pkg/trusted-data && mkdir -p pkg/trusted-data/etc && cp configs/platform/config.dev.server.yaml pkg/trusted-data/etc/config.yaml",
    }]
}
