---
- name: Install K3s
  shell: "curl -sfL https://get.k3s.io | sh | tee -a /home/{{ user_name }}/install.log"
  args:
    chdir: /tmp

- name: Restart K3s
  systemd:
    name: k3s
    state: restarted
    enabled: yes
