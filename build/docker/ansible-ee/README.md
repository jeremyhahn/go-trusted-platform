# Ansible Execution Environment

This is the Ansible Execution Environment for the Trusted Platform. It contains all of the necessary packages needed to support the platform playbooks and roles.

The included `Makefile` can be used to build the Execution Environment. Simply run the default `make` target to build a local ansible-ee container.
